<?php

declare(strict_types=1);

namespace CyrilVerloop\Codingame\Tests\{{ configuration.namespace }};

use CyrilVerloop\Codingame\Tests\PuzzleTestCase;
use CyrilVerloop\Codingame\{{ configuration.namespace }}\{{ configuration.class }};
use PHPUnit\Framework\Attributes as PA;

/**
 * Tests for the "{{ configuration.name }}" puzzle.
 */
#[
    PA\CoversClass({{ configuration.class }}::class),
    PA\Group('{{ configuration.group }}'),
    PA\Medium
]
final class CGTest extends PuzzleTestCase
{
    public function setUp(): void
    {
        $this->puzzle = new {{ configuration.class }}();
    }
{% for test in configuration.testsConfigurations %}

    /**
     * Test that the code can be executed for "{{ test.name }}".
     */
    #[PA\Group('{{ configuration.group }}_{{ test.group }}')]
    public function testCanExecute{{ test.method }}(): void
    {
        $this->expectExecuteOutputAnswer(
            __DIR__ . '/input/{{ test.file }}',
            file_get_contents(__DIR__ . '/output/{{ test.file }}')
        );
    }
{% endfor %}
}
