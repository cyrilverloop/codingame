fscanf($stdin, "%d", $N);
for ($i = 0; $i < $N; $i++)
{
    fscanf($stdin, "%d %d", $A, $B);
}
for ($i = 0; $i < $N; $i++)
{

    // Write an answer using echo(). DON'T FORGET THE TRAILING \n

    echo("9 20\n");
}