fscanf($stdin, "%d", $V);
fscanf($stdin, "%d", $N);
for ($i = 0; $i < $N; $i++)
{
    $FNAME = stream_get_line($stdin, 500 + 1, "\n");
}

// Write an answer using echo(). DON'T FORGET THE TRAILING \n

echo("answer\n");