fscanf($stdin, "%d", $T);
for ($i = 0; $i < $T; $i++)
{
    fscanf($stdin, "%d %d", $K, $N);
}
for ($i = 0; $i < $T; $i++)
{

    // Write an answer using echo(). DON'T FORGET THE TRAILING \n

    echo("W\n");
}