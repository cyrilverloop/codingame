fscanf($stdin, "%d", $N);
for ($i = 0; $i < $N; $i++)
{
    fscanf($stdin, "%s %d %d %d", $SHIP, $HP, $ARMOR, $DAMAGE);
}

// Write an answer using echo(). DON'T FORGET THE TRAILING \n

echo("PEWPEW\n");