fscanf($stdin, "%d", $L);
fscanf($stdin, "%d", $N);
for ($i = 0; $i < $N; $i++)
{
    $R = stream_get_line($stdin, 255 + 1, "\n");
}

// Write an answer using echo(). DON'T FORGET THE TRAILING \n

echo("result\n");