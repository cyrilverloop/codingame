fscanf($stdin, "%d", $N);
for ($i = 0; $i < $N; $i++)
{
    fscanf($stdin, "%s", $word);
}
for ($i = 0; $i < $N; $i++)
{

    // Write an answer using echo(). DON'T FORGET THE TRAILING \n

    echo("<word_prefix>\n");
}