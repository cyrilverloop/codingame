fscanf($stdin, "%d", $n);
for ($i = 0; $i < $n; $i++)
{
    $key = stream_get_line($stdin, 2 + 1, "\n");
}
for ($i = 0; $i < $n; $i++)
{

    // Write an answer using echo(). DON'T FORGET THE TRAILING \n

    echo("answer\n");
}