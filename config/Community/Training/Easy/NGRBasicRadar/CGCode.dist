fscanf($stdin, "%d", $N);
for ($i = 0; $i < $N; $i++)
{
    fscanf($stdin, "%s %s %d", $plate, $radarname, $timestamp);
}

// Write an answer using echo(). DON'T FORGET THE TRAILING \n

echo("ticket\n");