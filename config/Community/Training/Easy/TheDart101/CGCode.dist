fscanf($stdin, "%d", $N);
for ($i = 0; $i < $N; $i++)
{
    $PLAYER = stream_get_line($stdin, 1024 + 1, "\n");
}
for ($i = 0; $i < $N; $i++)
{
    $SHOOTS = stream_get_line($stdin, 1024 + 1, "\n");
}

// Write an answer using echo(). DON'T FORGET THE TRAILING \n

echo("winner\n");