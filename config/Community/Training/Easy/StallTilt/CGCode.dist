fscanf($stdin, "%d", $n);
fscanf($stdin, "%d", $v);
for ($i = 0; $i < $n; $i++)
{
    fscanf($stdin, "%d", $speed);
}
for ($i = 0; $i < $v; $i++)
{
    fscanf($stdin, "%d", $bends);
}

// Write an answer using echo(). DON'T FORGET THE TRAILING \n

echo("answer\n");