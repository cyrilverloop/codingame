fscanf($stdin, "%d", $N);
for ($i = 0; $i < $N; $i++)
{
    $line = stream_get_line($stdin, 255 + 1, "\n");
}
for ($i = 0; $i < $N; $i++)
{

    // Write an answer using echo(). DON'T FORGET THE TRAILING \n

    echo("answer\n");
}